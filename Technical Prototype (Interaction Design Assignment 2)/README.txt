Recommend using vscode to run
Made in Python 3.12.8

requires running "pip install google-cloud-vision"

the API key is in service-account-key.json.
This will not run without the JSON file. It must be in the same folder as the .py script.

The JSON is omitted from the GitHub repo version.
Check Blackboard for the version with the key.

